{
  "builds": [
    {
      "src": "./src/main.rs",
      "use": "vercel-rust@4.0.2 && cargo run build",
      "buildCommand": "cargo run"
    }
  ],
  "routes": [
    {"src": "/(.*)", "dest": "./src/main.py"}
  ]
}
