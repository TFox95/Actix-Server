{
  "builds": [
    {"src": "src/main.rs", "use": "vercel-rust@4.0.2"}
  ],
  "routes": [
    {"src": "/(.*)", "dest": "./src/main.py"}
  ],
  "buildCommand": "cargo build --bin Actix-Server --quiet --release"

}
